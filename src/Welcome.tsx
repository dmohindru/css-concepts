import { Box, Typography } from "@mui/material";
const Welcome: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">Welcome to the CSS Concept project</Typography>
    </Box>
  );
};

export default Welcome;
